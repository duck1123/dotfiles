# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set                     $super Mod4
set                     $hyper Mod3
font                    pango:DejaVu Sans Mono 8
# font -schumacher-clean-medium-r-normal-*-12-*-*-*-*-*-*-*

exec --no-startup-id rofi --daemon
exec --no-startup-id gnome-settings-daemon
exec --no-startup-id gnome-session
exec --no-startup-id compton -b -c --backend xrender
exec --no-startup-id ssh-add
# exec --no-startup-id xsetroot -solid "#002244"
exec --no-startup-id hsetroot -solid "#002244"
# exec --no-startup-id dropbox
# exec --no-startup-id keepass2
# exec --no-startup-id owncloud

# Use Mouse+$super to drag floating windows to their wanted position
floating_modifier       $super

# define names for workspaces with some nice symbols
set $w0  ✪
set $w1  ❶
set $w2  ❷
set $w3  ❸
set $w4  ❹
set $w5  ❺
set $w6  ❻
set $w7  ❼
set $w8  ❽
set $w9  ❾
set $w10 ❿

# resize window (you can also use the mouse for that)
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode
  # Pressing left will shrink the window’s width.
  # Pressing right will grow the window’s width.
  # Pressing up will shrink the window’s height.
  # Pressing down will grow the window’s height.
  bindsym         j resize shrink width 10 px or 10 ppt
  bindsym         k resize grow height 10 px or 10 ppt
  bindsym         l resize shrink height 10 px or 10 ppt
  bindsym         semicolon resize grow width 10 px or 10 ppt

  # same bindings, but for the arrow keys
  bindsym         Left resize shrink width 10 px or 10 ppt
  bindsym         Down resize grow height 10 px or 10 ppt
  bindsym         Up resize shrink height 10 px or 10 ppt
  bindsym         Right resize grow width 10 px or 10 ppt

  # back to normal: Enter or Escape
  bindsym         Return mode "default"
  bindsym         Escape mode "default"
}

bindsym $super+1               workspace $w0
bindsym $super+Shift+1         move container to workspace $w0
bindsym $super+2               workspace $w2
bindsym $super+Shift+2         move container to workspace $w2
bindsym $super+3               workspace $w3
bindsym $super+Shift+3         move container to workspace $w3
bindsym $super+4               workspace $w4
bindsym $super+Shift+4         move container to workspace $w4
bindsym $super+5               workspace $w5
bindsym $super+Shift+5         move container to workspace $w5
bindsym $super+6               workspace $w6
bindsym $super+Shift+6         move container to workspace $w6
bindsym $super+7               workspace $w7
bindsym $super+Shift+7         move container to workspace $w7
bindsym $super+8               workspace $w8
bindsym $super+Shift+8         move container to workspace $w8
bindsym $super+9               workspace $w9
bindsym $super+Shift+9         move container to workspace $w9
bindsym $super+0               workspace $w10
bindsym $super+Shift+0         move container to workspace $w10
bindsym $super+a               focus parent
bindsym $super+Shift+a         focus child
bindsym Ctrl+Alt+a             exec mono /usr/lib/keepass2/KeePass.exe --auto-type
bindsym $super+Shift+c         reload
bindsym $super+e               layout toggle split
bindsym $super+Shift+e         exec ~/.i3/bin/nag-exit
bindsym $super+f               fullscreen
bindsym $super+g               exec ~/.i3/bin/i3-choose-workspace
bindsym $super+h               split h
# bindsym $super+j               focus left
# bindsym $super+Shift+j         move left
# bindsym $super+k               focus down
# bindsym $super+Shift+k         move down
# bindsym $super+l               focus up
# bindsym $super+l               exec i3lock
bindsym $super+l               exec gnome-screensaver-command -l
# bindsym $super+l               exec xtrlock
# bindsym $super+Shift+l         move up
bindsym $super+m               exec clerk
bindsym $hyper+n             exec mpc next
bindsym $hyper+p             exec vlc http://dev.jiksnu.com:8000/
bindsym $super+Shift+q         kill
bindsym $super+r               mode "resize"
bindsym $super+Shift+r         restart
bindsym $super+s               layout stacking
bindsym $super+v               split v
bindsym $super+w               layout tabbed
# bindsym $super+space           exec --no-startup-id i3-dmenu-desktop --dmenu='rofi -dmenu -fg "#9f9f9f" -bg "#2c2c2e" -hlfg "#2c2c2e" -hlbg "#ccdc90" -o 85'
bindsym $super+space           exec --no-startup-id i3-dmenu-desktop --dmenu='rofi -dmenu'
bindsym $super+Shift+space     floating toggle
bindsym $super+Return          exec gnome-terminal
bindsym $super+Shift+Return    exec emacsclient -a '' -c
bindsym $super+Ctrl+Return     exec google-chrome
bindsym $super+semicolon       exec ~/.i3/bin/i3-prompt
# bindsym $super+semicolon       focus right
# bindsym $super+Shift+semicolon move right
bindsym $super+Down            focus down
bindsym $super+Shift+Down      move down
bindsym $hyper+Down          move down
bindsym $super+Left            focus left
bindsym $super+Shift+Left      move left
bindsym $hyper+Left          move left
bindsym $super+Right           focus right
bindsym $super+Shift+Right     move right
bindsym $hyper+Right         move right
bindsym $super+Up              focus up
bindsym $super+Shift+Up        move up
bindsym $hyper+Up            move up
# bindsym F12                  exec --no-startup-id rofi -now F12
# bindsym F11                  exec --no-startup-id rofi -rnow F11
bindsym XF86AudioLowerVolume exec /usr/bin/pactl set-sink-volume 0 '-5%'
bindsym XF86AudioRaiseVolume exec /usr/bin/pactl set-sink-volume 0 '+5%'
bindsym $hyper+plus gaps inner all plus 5
bindsym $hyper+minus gaps inner all minus 5

# list open windows to switch to
# bindsym $super+Tab exec rofi -i3 -now -font "Inconsolata-10" -fg "#9f9f9f" -bg "#2c2c2e" -hlfg "#2c2c2e" -hlbg "#ccdc90" -o 85

# change focus between tiling / floating windows
# bindsym                 $super+space focus mode_toggle

bar {
    i3bar_command i3bar -t

    colors {
        # Whole color settings
        background #000000
        statusline #ffffff
        separator  #666666

        # Type             border  background font
        focused_workspace  #008fff #007fff #ffffff
        active_workspace   #333333 #5f676a #ffffff
        inactive_workspace #333333 #222222 #888888
        urgent_workspace   #aa0000 #990000 #ffffff
    }

    font                    pango:DejaVu Sans Mono 10
    status_command i3status --config ~/.i3/i3status.conf
    # status_command ~/.i3/.i3status.sh
    position top
}

for_window [class="^.*"] border pixel 5
smart_borders on

gaps inner 15
gaps outer 0
